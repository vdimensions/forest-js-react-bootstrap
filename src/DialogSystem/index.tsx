import React from 'react';
import { Region, ForestView } from '@vdimensions/forest-js-react';

export default ForestView("DialogSystem", () => {
    return (
        <Region name="DialogArea"/>
    );
});
